package com.github.syr0ws.craftventory.api.config;

/**
 * Represents a component of an inventory configuration.
 * <p>
 * All classes that are part of an inventory configuration must implement this interface.
 * A configuration should also have immutable attributes as its goal is only to provide the loaded inventory
 * properties to the transformation process.
 * </p>
 */
public interface Configuration {
}
